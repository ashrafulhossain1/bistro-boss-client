/**
 * 1. do not show the links to those user who should not see the link
 * 2. even if they get the link , don't allow to execute this link (by Admin Route)
 * 3. don't allow the users to access the api's (check admin in these server as well)=> verify admin middleware
 * 
 */